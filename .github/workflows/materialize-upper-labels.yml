name: Prepare upper-label changes
on:
  push:
    branches: [fix/helper-permissions-upper-labels]
permissions:
  contents: write
jobs:
  prepare:
    if: github.repository == 'Ripthulhu/LG-XMB' && github.event.head_commit.message == 'Reconcile upper labels with the merged helper fix'
    runs-on: ubuntu-24.04
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - name: Apply exact source changes
        run: |
          python3 - <<'PY'
          import gzip, hashlib, json
          from pathlib import Path
          payload = Path('.maintenance/upper-labels.json.gz').read_bytes()
          assert hashlib.sha256(payload).hexdigest() == '2dc979f0980d5edf657857c9db6e1e3ec103fd0037080b276eb7362a74c945f1'
          for entry in json.loads(gzip.decompress(payload)):
              path = Path(entry['path'])
              assert not path.is_absolute() and '..' not in path.parts and path.parts[0] != '.github'
              if entry['before'] is None:
                  assert not path.exists()
                  text = ''
              else:
                  text = path.read_text()
                  assert hashlib.sha256(text.encode()).hexdigest() == entry['before'], str(path)
              for start, end, replacement in reversed(entry['changes']):
                  text = text[:start] + replacement + text[end:]
              assert hashlib.sha256(text.encode()).hexdigest() == entry['after'], str(path)
              path.parent.mkdir(parents=True, exist_ok=True)
              path.write_text(text)
          PY
          git diff --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Retain text on upper XMB items for 0.1.14'
          git push origin HEAD:refs/heads/fix/helper-permissions-upper-labels
