# 0.1.27 (test build)

- Optional hardware MSAA (Off/2×/4×) on the cropped wave surface; off by default.
- WebGL 2 preferred, WebGL 1 and Canvas2D fallbacks retained.
- Query actual RGBA8 sample counts and report unsupported/allocation/resolve
  fallback without silently increasing samples or retrying each frame.
- Keep supersampling, FXAA and all existing colour/particle controls independent.
- Correct fractional-sampling alignment, crop allocation churn and particle
  viewport restoration in the unpublished 0.1.26 crop.
- Restore display-colour FXAA edge detection on the cropped composite.
- Keep the helper, native controls, music path and recordings unchanged.

Local browser testing is not a TV performance test. Before release, run the
normal URL/CSP and complete regression suite, then test on supported TVs.
