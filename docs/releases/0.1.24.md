# 0.1.24 candidate

- Raise waves and particles another 10% of the output height (108 px at 1080p).
- Synchronize the category bar, column slide/crossfade and category-icon emphasis
  with one 180 ms timeline. Retain rendered positions during rapid reversals;
  clicking a distant category moves the correct distance. Reduced motion,
  immediate activation and cleanup on exit remain supported.
- Use `/media/internal/lg-xmb/background.mp3` consistently in setup, UI and docs.
  Repoint only the recognized old app link. Do not move or overwrite music;
  no recording is distributed.

The selected-item glow remains removed. Root helper concurrency, remote mapping,
background restrictions, capture and wave quality controls are retained.
This is a test build; on-TV verification is still required.
