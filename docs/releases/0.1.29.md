# LG-XMB 0.1.29

- Replace the category swipe/reveal with the same 160 ms CSS transform timing
  used by up/down navigation. The horizontal bar retains its final transform;
  active category icons scale with the same timing as vertical item icons.
- Keep the vertical list at its resting X position and opacity throughout
  category changes. No animation-end transform/layer cleanup shifts its icons.
- Let CSS retarget rapid reversals. Remove the scripted animations, progress
  reads and completion timers; retain row reuse and immediate selection.
- Leave waves, MSAA, FXAA, particles, colours, music and native controls unchanged.
  The helper controller changes only its version-specific manifest pin.

Install over the existing app without clearing settings or helper state.
On-TV appearance and performance of the revised navigation need confirmation;
no TV frame-rate gain is claimed from desktop tests.
